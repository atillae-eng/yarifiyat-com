import { NextResponse } from 'next/server'; import { calculateTKS } from '@/lib/tksAlgorithm'; export async function POST(request: Request) { try { const body = await request.json(); const { discount, benefit, contribution, trust } = body; const score = calculateTKS({ discount, benefit, contribution, trust }); return NextResponse.json({ tks_score: score }); } catch { return NextResponse.json({ error: 'Hata' }, { status: 500 }); } }
